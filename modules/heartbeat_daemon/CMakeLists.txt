if(CONFIG_MODULES_HEARTBEAT_DAEMON)
    nuttx_add_application(
        NAME
        ${CONFIG_MODULES_HEARTBEAT_DAEMON_PROGNAME}
        PRIORITY
        ${CONFIG_MODULES_HEARTBEAT_DAEMON_PRIORITY}
        STACKSIZE
        ${CONFIG_MODULES_HEARTBEAT_DAEMON_STACKSIZE}
        MODULE
        ${CONFIG_MODULES_HEARTBEAT_DAEMON}
        SRCS
        heartbeat_daemon_main.cpp)
endif()
