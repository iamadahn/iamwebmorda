include $(APPDIR)/Make.defs

PROGNAME = $(CONFIG_MODULES_HEARTBEAT_DAEMON_PROGNAME)
PRIORITY = $(CONFIG_MODULES_HEARTBEAT_DAEMON_PRIORITY)
STACKSIZE = $(CONFIG_MODULES_HEARTBEAT_DAEMON_STACKSIZE)
MODULE = $(CONFIG_MODULES_HEARTBEAT_DAEMON)

CXXEXT = .cpp
MAINSRC = heartbeat_daemon_main.cpp

include $(APPDIR)/Application.mk
